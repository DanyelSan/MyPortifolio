# Apague o arquivo antigo
rm requirements.txt 

# Gere um novo apenas com o essencial (ou edite manualmente)
echo "flask==3.0.3" >> requirements.txt
echo "flask-mail==0.10.0" >> requirements.txt
echo "python-dotenv==1.0.1" >> requirements.txt
echo "gunicorn==22.0.0" >> requirements.txt
